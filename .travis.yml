sudo: false
language: node_js
node_js:
  - "8"
  - "12"

before_install:
  - curl https://install.meteor.com | sh

before_script:
  - "export PATH=$HOME/.meteor:$PATH"

env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
