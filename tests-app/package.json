{
    "name": "tests",
    "private": true,
    "scripts": {
      "test": "METEOR_PACKAGE_DIRS=../packages TEST_BROWSER_DRIVER=puppeteer meteor test --once --driver-package meteortesting:mocha --raw-logs",
      "test:watch": "METEOR_PACKAGE_DIRS=../packages TEST_BROWSER_DRIVER=puppeteer TEST_WATCH=1 meteor test --driver-package meteortesting:mocha --raw-logs",
      "test:watch:browser": "METEOR_PACKAGE_DIRS=../packages TEST_WATCH=1 meteor test --driver-package meteortesting:mocha --raw-logs"
    },
    "dependencies": {
        "@babel/runtime": "^7.28.6",
        "jquery": "^3.7.1",
        "meteor-node-stubs": "^1.2.26"
    },
    "devDependencies": {
      "puppeteer": "^24.37.5",
      "expect": "^26.6.2"
    }
  }